[build-system]
requires = ["setuptools>=75", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "webmark"
version = "0.1.0"
description = "URL to Raw Markdown collection skill for WorkBuddy"
requires-python = ">=3.11"
dependencies = [
  "beautifulsoup4>=4.12,<5",
  "markdownify>=0.14,<1",
  "PyYAML>=6.0,<7",
  "requests>=2.32,<3",
]

[project.optional-dependencies]
test = ["pytest>=8,<9"]

[project.scripts]
webmark = "webmark.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
